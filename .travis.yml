sudo: false

language: python

python:
  - 3.5

install:
  - pip install -r requirements.txt
  - pip install codecov

script:
  - make test
  # - make lint # disabled due to error in pylint

after_success:
- codecov
