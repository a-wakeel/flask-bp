sudo: false

language: python

python:
  - 3.5

install:
  - pip install poetry
  - pip install codecov
  - poetry install

script:
  - make test
  # - make lint # disabled due to error in pylint

after_success:
- codecov
